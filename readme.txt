Validations
- user can add only 0 to 1 argument when starting app from the command line
- if the file with payments contains invalid payment this payment is not imported but the app contains to work
- if the file with payments doesn't exist text is shown to the user andCK the app contains to work
- if the user input is invalid text is shown to the user
- the user input is trimmed before validation